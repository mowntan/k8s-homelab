{{- if and .Values.vpn.enabled .Values.vpn.auth }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "qbittorrent.fullname" . }}-vpn-auth
  labels:
    {{- include "qbittorrent.labels" . | nindent 4 }}
type: Opaque
stringData:
  auth: |
    {{- .Values.vpn.auth | nindent 4 }}
{{- end }}

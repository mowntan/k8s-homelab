name: Check for new Ollama release

on:
  schedule:
    - cron: '0 6 * * *'  # Daily at 06:00 UTC
  workflow_dispatch:

jobs:
  check-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get latest Ollama release
        id: ollama
        run: |
          LATEST=$(curl -fsSL https://api.github.com/repos/ollama/ollama/releases/latest | jq -r '.tag_name' | sed 's/^v//')
          echo "latest=${LATEST}" >> "${GITHUB_OUTPUT}"

      - name: Get current version
        id: current
        run: |
          CURRENT=$(cat apps/ollama/VERSION | tr -d '[:space:]')
          echo "current=${CURRENT}" >> "${GITHUB_OUTPUT}"

      - name: Log version comparison
        run: |
          echo "Current: ${{ steps.current.outputs.current }}"
          echo "Latest:  ${{ steps.ollama.outputs.latest }}"

      - name: Update VERSION file
        if: steps.ollama.outputs.latest != steps.current.outputs.current
        run: echo "${{ steps.ollama.outputs.latest }}" > apps/ollama/VERSION

      - name: Open PR with version bump
        if: steps.ollama.outputs.latest != steps.current.outputs.current
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: bump Ollama to ${{ steps.ollama.outputs.latest }}"
          branch: "update/ollama-${{ steps.ollama.outputs.latest }}"
          title: "chore: bump Ollama to ${{ steps.ollama.outputs.latest }}"
          body: |
            Automated version bump for Ollama.

            | | Version |
            |---|---|
            | Previous | `${{ steps.current.outputs.current }}` |
            | New | `${{ steps.ollama.outputs.latest }}` |

            Merging this PR will trigger the Docker image build workflow.

            [Ollama ${{ steps.ollama.outputs.latest }} release notes](https://github.com/ollama/ollama/releases/tag/v${{ steps.ollama.outputs.latest }})
          labels: automated,dependency-update
